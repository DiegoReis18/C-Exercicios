#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(){
	int NUM[] = {1,2,3};
	int elem = sizeof(NUM)/sizeof(int);
	printf("Tamanho: %d",sizeof(NUM));
	printf("\nTamanho: %d",elem);
	
}